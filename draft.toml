[environments]
  [environments.development]
    name = "kube-secrets"
    namespace = "supersecrets"
    wait = true
    watch = false
    watch-delay = 2
    auto-connect = false
    dockerfile = ""
    chart = ""
